services:
    portfolio:
        build:
            context: .
        ports:
            - '80:5173'
        volumes:
            - .:/app
            - /app/node_modules
        stdin_open: true
        tty: true
